services:
  polarion-mcp:
    image: peakflames/polarion-remote-mcp-server
    ports:
      - "8080:8080"
    volumes:
      - ./appsettings.json:/app/appsettings.json
    env_file:
      - .env
    environment:
      - POLARION_PASSWORD=${PALARION_PAT}
    restart: unless-stopped
